// Loop with break (multiple successors/predecessors)
def main() {
  var i = 0 : int;
  var sum = 0 : int;
  while (i < 10) {
    if (i == 5) { break; }
    sum = sum + i;
    i = i + 1;
  }
  print(sum);
}
