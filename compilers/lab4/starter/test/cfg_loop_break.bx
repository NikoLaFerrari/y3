def main() {
    var i = 0 : int;
    var sum = 0 : int;

    while (i < 10) {
        if (i == 5) {
            break; // Creates an edge leaving the loop immediately
        }
        sum = sum + i;
        i = i + 1; // Creates a back-edge to the start of while
    }
    print(sum);
}
