
def outer(x : int) : int {
    def middle(f : function(int) -> int) : int {
        return f(x);
    }
    
    def inner(n : int) : int {
        return n * 2;
    }
    
    return middle(inner);
}

def main() {
    print(outer(7));
}
