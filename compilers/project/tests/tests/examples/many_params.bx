
// Test procedure with many parameters
def compute_formula(a, b, c, d, e : int) : int {
  return (a + b) * c - d / e;
}

def main() {
  print(compute_formula(10, 20, 3, 100, 5));
  print(compute_formula(5, 5, 2, 50, 10));
}
