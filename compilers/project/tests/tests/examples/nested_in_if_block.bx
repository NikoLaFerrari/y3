
def main() {
    var x = 5 : int;
    
    def nested_in_if() : int {
        return x;
    }
    
    if (x > 0) {
        def nested_in_if_block() : int {
            return x * 2;
        }
        print(nested_in_if_block());
    }
    
    print(nested_in_if());
}
