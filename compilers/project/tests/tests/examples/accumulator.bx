
def main() {
    var result = 0 : int;
    
    def accumulate(n : int) {
        result = result + n;
    }
    
    accumulate(5);
    accumulate(10);
    accumulate(15);
    
    print(result);
}
