
def test(f : function(int, int, int) -> int, a : int, b : int, c : int) : int {
    return f(a, b, c);
}

def main() {
    def sum3(x : int, y : int, z : int) : int {
        return x + y + z;
    }
    
    print(test(sum3, 10, 20, 30));
}
