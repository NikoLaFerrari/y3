
// Test recursive function
def factorial(n : int) : int {
  if (n <= 1) {
    return 1;
  } else {
    return n * factorial(n - 1);
  }
  return 0;
}

def main() {
  print(factorial(5));
  print(factorial(7));
  print(factorial(10));
}
