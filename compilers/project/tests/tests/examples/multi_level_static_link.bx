
def outer_func(x : int) : int {
    def middle(y : int) : int {
        def inner() : int {
            return x + y;
        }
        return inner();
    }
    return middle(5);
}

def main() {
    print(outer_func(10));
}
