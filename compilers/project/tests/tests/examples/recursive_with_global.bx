// Test combination of recursion and globals

var call_count = 0 : int;

def countdown(n : int) {
  call_count = call_count + 1;
  if (n > 0) {
    print(n);
    countdown(n - 1);
  }
}

def main() {
  countdown(5);
  print(call_count);
}
