
def do_action(action : function(int) -> void, x : int) {
    action(x);
}

def main() {
    var result = 0 : int;
    
    def set_result(n : int) {
        result = n;
    }
    
    do_action(set_result, 42);
    print(result);
}
