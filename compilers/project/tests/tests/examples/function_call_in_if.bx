
def apply(f : function(int) -> int, x : int) : int {
    return f(x);
}

def main() {
    var y = 10 : int;
    
    def add_y(n : int) : int {
        return n + y;
    }
    
    if (y > 5) {
        print(apply(add_y, 15));
    } else {
        print(0);
    }
}
