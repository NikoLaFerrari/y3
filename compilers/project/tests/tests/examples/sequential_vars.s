    .data
    .text
    .globl main

main:
    pushq %rbp
    movq %rsp, %rbp
    subq $368, %rsp
    movq $1, %rax
    movq %rax, -56(%rbp)
    movq %rbp, %rax
    movq -56(%rbp), %rcx
    movq %rcx, -8(%rax)
    movq $2, %rax
    movq %rax, -64(%rbp)
    movq %rbp, %rax
    movq -64(%rbp), %rcx
    movq %rcx, -16(%rax)
    movq $3, %rax
    movq %rax, -72(%rbp)
    movq %rbp, %rax
    movq -72(%rbp), %rcx
    movq %rcx, -24(%rax)
    movq $4, %rax
    movq %rax, -80(%rbp)
    movq %rbp, %rax
    movq -80(%rbp), %rcx
    movq %rcx, -32(%rax)
    movq $5, %rax
    movq %rax, -88(%rbp)
    movq %rbp, %rax
    movq -88(%rbp), %rcx
    movq %rcx, -40(%rax)
    movq %rbp, %rax
    movq -8(%rax), %rcx
    movq %rcx, -96(%rbp)
    movq %rbp, %rax
    movq -16(%rax), %rcx
    movq %rcx, -104(%rbp)
    movq -96(%rbp), %rax
    movq -104(%rbp), %rcx
    addq %rcx, %rax
    movq %rax, -112(%rbp)
    movq %rbp, %rax
    movq -24(%rax), %rcx
    movq %rcx, -120(%rbp)
    movq -112(%rbp), %rax
    movq -120(%rbp), %rcx
    addq %rcx, %rax
    movq %rax, -128(%rbp)
    movq %rbp, %rax
    movq -32(%rax), %rcx
    movq %rcx, -136(%rbp)
    movq -128(%rbp), %rax
    movq -136(%rbp), %rcx
    addq %rcx, %rax
    movq %rax, -144(%rbp)
    movq %rbp, %rax
    movq -40(%rax), %rcx
    movq %rcx, -152(%rbp)
    movq -144(%rbp), %rax
    movq -152(%rbp), %rcx
    addq %rcx, %rax
    movq %rax, -160(%rbp)
    movq %rbp, %rax
    movq -160(%rbp), %rcx
    movq %rcx, -48(%rax)
    movq %rbp, %rax
    movq -48(%rax), %rcx
    movq %rcx, -168(%rbp)
    leaq __bx_print_int(%rip), %rax
    movq $0, %r10
    pushq %r10
    pushq $0
    movq -168(%rbp), %rdi
    call *%rax
    addq $16, %rsp
    movq %rbp, %rax
    movq -8(%rax), %rcx
    movq %rcx, -176(%rbp)
    movq $2, %rax
    movq %rax, -184(%rbp)
    movq -176(%rbp), %rax
    movq -184(%rbp), %rcx
    imulq %rcx, %rax
    movq %rax, -192(%rbp)
    movq %rbp, %rax
    movq -192(%rbp), %rcx
    movq %rcx, -8(%rax)
    movq %rbp, %rax
    movq -16(%rax), %rcx
    movq %rcx, -200(%rbp)
    movq $2, %rax
    movq %rax, -208(%rbp)
    movq -200(%rbp), %rax
    movq -208(%rbp), %rcx
    imulq %rcx, %rax
    movq %rax, -216(%rbp)
    movq %rbp, %rax
    movq -216(%rbp), %rcx
    movq %rcx, -16(%rax)
    movq %rbp, %rax
    movq -24(%rax), %rcx
    movq %rcx, -224(%rbp)
    movq $2, %rax
    movq %rax, -232(%rbp)
    movq -224(%rbp), %rax
    movq -232(%rbp), %rcx
    imulq %rcx, %rax
    movq %rax, -240(%rbp)
    movq %rbp, %rax
    movq -240(%rbp), %rcx
    movq %rcx, -24(%rax)
    movq %rbp, %rax
    movq -32(%rax), %rcx
    movq %rcx, -248(%rbp)
    movq $2, %rax
    movq %rax, -256(%rbp)
    movq -248(%rbp), %rax
    movq -256(%rbp), %rcx
    imulq %rcx, %rax
    movq %rax, -264(%rbp)
    movq %rbp, %rax
    movq -264(%rbp), %rcx
    movq %rcx, -32(%rax)
    movq %rbp, %rax
    movq -40(%rax), %rcx
    movq %rcx, -272(%rbp)
    movq $2, %rax
    movq %rax, -280(%rbp)
    movq -272(%rbp), %rax
    movq -280(%rbp), %rcx
    imulq %rcx, %rax
    movq %rax, -288(%rbp)
    movq %rbp, %rax
    movq -288(%rbp), %rcx
    movq %rcx, -40(%rax)
    movq %rbp, %rax
    movq -8(%rax), %rcx
    movq %rcx, -296(%rbp)
    movq %rbp, %rax
    movq -16(%rax), %rcx
    movq %rcx, -304(%rbp)
    movq -296(%rbp), %rax
    movq -304(%rbp), %rcx
    addq %rcx, %rax
    movq %rax, -312(%rbp)
    movq %rbp, %rax
    movq -24(%rax), %rcx
    movq %rcx, -320(%rbp)
    movq -312(%rbp), %rax
    movq -320(%rbp), %rcx
    addq %rcx, %rax
    movq %rax, -328(%rbp)
    movq %rbp, %rax
    movq -32(%rax), %rcx
    movq %rcx, -336(%rbp)
    movq -328(%rbp), %rax
    movq -336(%rbp), %rcx
    addq %rcx, %rax
    movq %rax, -344(%rbp)
    movq %rbp, %rax
    movq -40(%rax), %rcx
    movq %rcx, -352(%rbp)
    movq -344(%rbp), %rax
    movq -352(%rbp), %rcx
    addq %rcx, %rax
    movq %rax, -360(%rbp)
    movq %rbp, %rax
    movq -360(%rbp), %rcx
    movq %rcx, -48(%rax)
    movq %rbp, %rax
    movq -48(%rax), %rcx
    movq %rcx, -368(%rbp)
    leaq __bx_print_int(%rip), %rax
    movq $0, %r10
    pushq %r10
    pushq $0
    movq -368(%rbp), %rdi
    call *%rax
    addq $16, %rsp
    movq $0, %rax
    leave
    ret
