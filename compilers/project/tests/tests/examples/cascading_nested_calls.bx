
def outer(x : int) : int {
    def middle(y : int) : int {
        def inner(z : int) : int {
            return x + y + z;
        }
        return inner(y + 1);
    }
    return middle(x + 1);
}

def main() {
    print(outer(5));
}
